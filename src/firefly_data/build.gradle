
sourceSets {
  main.java.srcDir '.'
  main.resources {
    srcDirs "."
    exclude "**/*.gradle"
  }
}

jar {
  archiveBaseName = 'firefly_data'
  includes = ['**']
  from sourceSets.main.allJava
}

clean {
  doFirst {
    delete "${jar.destinationDirectory}/${jar.archiveBaseName}.jar"
  }
}
