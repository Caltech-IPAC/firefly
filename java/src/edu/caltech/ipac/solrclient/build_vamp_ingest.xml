<project name="Spitzer solrclient.jar Build File" default="all" basedir=".">
    <description>
        The build file for building solrclient.jar
    </description>
<!-- Project level init file -->
    <property name="build.root" value="${basedir}/../../../../../"/>
    <import file="${build.root}/tools/init.xml"/>



    <target name="all" description="create vamp_ingest.prop file for SOLR ingestion process">
        <ant antfile="build.xml" target="ingest"/>

        <property file="${build.root}/../config/vamp/${env-file}"/>

        <echo file="${dist.dir}/vamp_ingest.prop">
solr.server.url=${solr.server.url}
db.driver=com.informix.jdbc.IfxDriver
db.url=${vamp.db.url}
db.userId=${vamp.db.userId}
db.password=${vamp.db.password}
doctype=vamp
mode=INDEX
sql=select distinct avm_meta_id id, avm_id, avm_type, publisher, publisher_id, instrument, facility, title, headline, subject_cat, subject_name, description, image_product_quality, credit, creator, contact_name from avm_meta;        </echo>

    </target>


    <target name="clean">
        <ant antfile="build.xml" target="clean"/>
        <delete file="${dist.dir}/vamp_ingest.prop"/>
     </target>

</project>
