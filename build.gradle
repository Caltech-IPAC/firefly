ext.fireflyPath = rootDir.path

apply from: "$fireflyPath/buildScript/init.gincl"
apply from: "$fireflyPath/buildScript/deployAll.gincl"

allprojects {
  defaultTasks 'build'

}

subprojects {
  apply plugin: "java"
  apply from: "$rootDir/buildScript/depends.gincl"
}

