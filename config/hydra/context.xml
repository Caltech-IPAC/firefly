<?xml version='1.0' encoding='utf-8'?>
<Context unpackWAR="true" reloadable="false" swallowOutput="true">

    <!-- maxActive: Maximum number of dB connections in pool. Make sure you
         configure your mysqld max_connections large enough to handle
         all of your db connections. Set to -1 for no limit.
         -->

    <!-- maxIdle: Maximum number of idle dB connections to retain in pool.
         Set to -1 for no limit.  See also the DBCP documentation on this
         and the minEvictableIdleTimeMillis configuration parameter.
         -->

    <!-- maxWait: Maximum time to wait for a dB connection to become available
         in ms, in this example 10 seconds. An Exception is thrown if
         this timeout is exceeded.  Set to -1 to wait indefinitely.
         -->

    <!-- username and password: dB username and password for dB connections
         -->

    <!-- driverClassName: Class name of JDBC drive
         -->

    <!-- url: The JDBC connection url
         -->

    <!-- declaring datasources for the dyn webapp -->
	<Resource name="jdbc/Operation" auth="Container"
	          type="javax.sql.DataSource" driverClassName="com.mysql.jdbc.Driver"
	          url="%operation.db.url%"
	          username="%operation.db.userId%" password="%operation.db.password%" maxActive="5" maxIdle="-1"
			  maxWait="10000" validationQuery="/* ping */" testOnBorrow="false" testOnReturn="false" testWhileIdle="true"
			  timeBetweenEvictionRunsMillis="3600000" minEvictableIdleTimeMillis="1800000" numTestsPerEvictionRun="-1"
	/>

	<Resource name="MailSession" auth="Container"
		      type="javax.mail.Session"
		      mail.transport.protocol="smtp"
		      mail.smtp.host= "mail0.ipac.caltech.edu"
		      mail.smtp.auth= "false"
		      mail.smtp.port= "587"
		      mail.smtp.from= "donotreply@ipac.caltech.edu"
		      mail.smtp.starttls.enable="true"
		      description="IRSA E-Mail Resource"
	/>

    <!--<Valve className="org.apache.catalina.valves.AccessLogValve"-->
           <!--prefix="heritage_access." suffix=".log"-->
           <!--pattern="common"/>-->

</Context>
